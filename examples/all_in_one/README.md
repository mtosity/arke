# All in One Example

This example shows how to produce and consume messages from the same application through Arke.

Before running the example, make sure both Arke and your message broker are already running.

Run the example from this directory with:

```sh
go run .
```
