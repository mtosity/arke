version: "3"
services:
  gencerts:
    build:
      dockerfile: Dockerfile.gencerts
      context: .
    image: tests_gencerts:latest
    volumes:
    - ./certs:/certs
    # environment:
    # - ADDITIONAL_HOSTNAMES=${ADDITIONAL_HOSTNAMES}
    command: ${ARKE_ADDITIONAL_HOSTNAMES}